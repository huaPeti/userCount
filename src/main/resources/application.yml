###server
server:
  port: 8080

###log
logging:
  config: classpath:logback-spring.xml
  file: count
##mybatis
mybatis:
  mapper-locations: classpath:/mapper/*.xml
##datasource
spring:
  datasource:
    url: jdbc:postgresql://49.4.14.245:8000/postgres?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull
    username: donotinputwrongpwd
    password: 123qwe!@#
    driver-class-name: org.postgresql.Driver
  freemarker:
    cache: false #为了热加载
    charset: utf-8
    content-type: text/html
    enabled: true
    settings:
      number_format: '0'
  mvc:
    static-path-pattern: /static/**


